services:
  - type: web
    name: company-dashboard
    env: node
    region: oregon
    plan: free
    buildCommand: npm install
    startCommand: node server-ultra-minimal.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production 